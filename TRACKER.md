# Tracker — Change Registry

Work status for the [study book](README.md). This is for managing the work; the [README](README.md) is for reading it.

**Progress: 56 / 59 written and passed the eval gate.** (Core book — 11 categories, 55 topics — complete; new **Category 12 — Agent engineering** added, 4 topics, 1 written: Tool design ☑. Remaining in Category 12: Harness engineering, Loop engineering, Memory architecture.)

> **Numbering:** lessons and category folders carry **no number prefix** — reading order lives only in the [README](README.md) table of contents, and lessons are referred to **by title**. Adding or reordering material never renumbers anything.

**Original milestone: 55 / 55 — core book complete.** (All 11 core categories complete and fully to standard: dependency-clean order, Mermaid diagrams, per-answer rationales, and the "one analogy to remember" section in every lesson. Cross-link integrity verified book-wide, 0 broken links.)

Legend: ☐ not started · ◧ drafting · ☑ written + passed `lesson-eval`

| Category | Topic | Status | Passed eval | Notes |
|---|-------|:------:|:-----------:|-------|
| Foundations | Tokenization and tokens | ☑ | ✅ | LEGO analogy |
| Foundations | Embeddings | ☑ | ✅ | city-map analogy |
| Foundations | Attention | ☑ | ✅ | cocktail-party analogy |
| Foundations | Transformers | ☑ | ✅ | reworked as capstone; editorial-team analogy |
| Foundations | Context windows | ☑ | ✅ | whiteboard analogy |
| Foundations | Pretraining vs. post-training | ☑ | ✅ | library-then-manners analogy |
| Model behavior & training | Fine-tuning | ☑ | ✅ | journalist house-style analogy |
| Model behavior & training | PEFT | ☑ | ✅ | glasses + clip-on analogy |
| Model behavior & training | LoRA | ☑ | ✅ | blueprint + tracing-paper analogy |
| Model behavior & training | Instruction tuning | ☑ | ✅ | new-hire + binder analogy |
| Model behavior & training | RLHF | ☑ | ✅ | chef taste-test analogy |
| Model behavior & training | DPO | ☑ | ✅ | editor's-folder analogy |
| Model behavior & training | Distillation | ☑ | ✅ | sommelier analogy |
| Model behavior & training | Quantization | ☑ | ✅ | price-list rounding analogy |
| Model behavior & training | Mixture of Experts | ☑ | ✅ | hospital-triage analogy |
| Reasoning & generation | Sampling and decoding | ☑ | ✅ | weighted-dice analogy |
| Reasoning & generation | Chain-of-thought and extended thinking | ☑ | ✅ | scratchpad / show-your-working analogy |
| Reasoning & generation | Reasoning models and test-time compute | ☑ | ✅ | chess study-vs-clock analogy |
| Reasoning & generation | Hallucination | ☑ | ✅ | never-says-"I-don't-know" trivia-friend analogy |
| Reasoning & generation | Structured outputs | ☑ | ✅ | fill-in-the-form analogy; completes category 3 |
| Retrieval & knowledge | RAG | ☑ | ✅ | open-book consultant analogy; category-4 opener |
| Retrieval & knowledge | Chunking | ☑ | ✅ | textbook-to-flashcards analogy |
| Retrieval & knowledge | Vector databases | ☑ | ✅ | coffee-shop map-app / ANN analogy |
| Retrieval & knowledge | Retrieval | ☑ | ✅ | recruiter/résumé analogy; similarity≠relevance |
| Retrieval & knowledge | Hybrid search | ☑ | ✅ | two-bookstore-clerks (meaning vs. exact-catalog) analogy; dense+sparse fusion, RRF |
| Retrieval & knowledge | Reranking | ☑ | ✅ | matchmaker profile-cards vs. real-dates analogy; bi- vs. cross-encoder |
| Retrieval & knowledge | Grounding and citations | ☑ | ✅ | newsroom standards-desk analogy; category-4 closer |
| Prompting | Prompt engineering | ☑ | ✅ | brilliant-literal-freelancer-brief analogy; category-5 opener |
| Prompting | Few-shot and in-context learning | ☑ | ✅ | temp-worker-with-sample-forms analogy |
| Prompting | KV caching | ☑ | ✅ | corkboard-of-index-cards analogy |
| Prompting | Prompt caching | ☑ | ✅ | prep-kitchen base-steps analogy |
| Agents | Tool calling / function calling | ☑ | ✅ | consultant + request-slip analogy; category 6 opener |
| Agents | MCP (Model Context Protocol) | ☑ | ✅ | USB-C-for-AI-tools analogy |
| Agents | Agentic systems | ☑ | ✅ | intern-with-a-goal vs. checklist analogy |
| Agents | Agent memory | ☑ | ✅ | Memento amnesiac + desk + filing-cabinet analogy |
| Agents | Planning and orchestration | ☑ | ✅ | five-city-trip itinerary vs. winging-each-leg analogy; ReAct vs. plan-and-execute |
| Agents | Multi-agent architectures | ☑ | ✅ | kitchen-brigade analogy; category 6 complete |
| Multimodal | Vision-language models | ☑ | ✅ | currency-exchange-booth analogy; category 7 opener |
| Multimodal | Audio and speech | ☑ | ✅ | sheet-music-vs-by-ear (jazz) analogy |
| Multimodal | Multimodal product surfaces | ☑ | ✅ | restaurant-ordering-channels analogy; category 7 closer |
| Safety & trust | Guardrails | ☑ | ✅ | bouncer + coat-check analogy; category-8 opener |
| Safety & trust | AI security | ☑ | ✅ | eager-temp-receptionist (no trust boundary) analogy |
| Safety & trust | Privacy and PII | ☑ | ✅ | friend-with-perfect-memory-who-gossips analogy |
| Safety & trust | Responsible AI | ☑ | ✅ | hospital ethics-board analogy; category 8 complete |
| Evaluation | Evaluation methods | ☑ | ✅ | multiple-choice-key vs. essay-rubric-panel analogy; category-9 opener; owns LLM-as-judge |
| Evaluation | Benchmarks and their limits | ☑ | ✅ | SAT / teaching-to-the-test analogy; contamination, saturation, construct validity, Goodhart |
| Evaluation | Regression testing | ☑ | ✅ | fix-one-pipe-then-test-every-tap analogy; prompts-as-code, evals-as-CI; category 9 complete |
| Production & ops | Production LLM architecture | ☑ | ✅ | airport (control tower / gates / baggage — the aircraft is one part of the operation) analogy; category-10 opener |
| Production & ops | Inference optimization | ☑ | ✅ | rush-hour-highway (one trip's time vs. cars-per-hour) analogy; latency↔throughput, continuous batching, speculative decoding, GQA/MQA/PagedAttention |
| Production & ops | LLM observability | ☑ | ✅ | car dashboard + dashcam analogy; traces/spans, live signals, online eval, drift; complements regression testing |
| Production & ops | Cost and unit economics | ☑ | ✅ | metered-electricity-bill analogy; per-token pricing + input/output asymmetry, marginal-cost-≠-0, unit economics, cost levers |
| Product & strategy | AI product metrics | ☑ | ✅ | car-spec-sheet-vs-commute analogy; category-11 opener; model metrics ≠ product metrics |
| Product & strategy | Build vs. buy | ☑ | ✅ | rent/buy/build-a-home analogy; two-layer (model vs. app) decision, buy-first-because-reversible, train-from-scratch trap |
| Product & strategy | Data moats and feedback loops | ☑ | ✅ | worn-footpath flywheel analogy; most data is not a moat (needs proprietary + hard-to-replicate + product-improving); cold start |
| Product & strategy | The iron triangle | ☑ | ✅ | three-fader mixing-board analogy; cost/latency/quality tradeoff; capstone; book complete |
| Agent engineering | Tool design | ☑ | ✅ | workshop-apprentice (reads only labels/cards) analogy; category-12 opener; four surfaces (name/description/schema/return), task-shaped vs endpoint-shaped granularity, don't-require-guessable-args, return signal-not-dump |
| Agent engineering | Harness engineering | ☐ | | — |
| Agent engineering | Loop engineering | ☐ | | — |
| Agent engineering | Memory architecture | ☐ | | — |

- **2026-09-13** — **De-numbered the whole book.** Dropped the `NN-` prefix from all 12 category folders (`01-foundations` → `foundations`, …) and all 56 lesson files (`07-fine-tuning.md` → `fine-tuning.md`, …), and from the 7 `dev-surfaces/` lessons. Every intra-book link rewritten to the numberless path; every in-prose "lesson NN" reference reworded to the lesson **title**; README ToC display numbers restart per section; this tracker's status table now keyed by **category + title** instead of a global `#`. The `learning/` radar tree keeps its own (chronological) numbering, but its links *into* lessons were repointed. Reason: global numbers cascaded on every insert/reorder — not scalable. New convention: reading order lives only in the README ToC; refer to lessons by title. (Changelog entries below predate this and keep their original `(NN)` references as a historical record.)

- **2026-09-12** — **Category 3 COMPLETE: Structured outputs (20)** written and passed eval (14/14). Scoped as the how-to-get-machine-readable-output topic: the free-text↔software mismatch; the ladder of guarantees (prompt-and-pray → JSON mode → schema-constrained → tool calling, with validate-and-retry as safety net); **constrained decoding** as the mechanism (mask logits so only schema-valid tokens survive — a mechanical guarantee, not a request); and the three traps — validity ≠ correctness (well-formatted hallucination), the format straitjacket (structure-while-reasoning hurts quality → reason-first / reasoning-field-first), and over-strict schemas *inducing* fabrication (required non-nullable fields force invented values). Tool calling (32), sampling/logits (16), CoT (17), hallucination (19) all linked, not taught. fill-in-the-form analogy. Category 3 (16–20) now fully on disk.

- **2026-09-12** — **Category 3 started. Lesson 16 (Sampling and decoding)** written and passed `lesson-eval` (14/14). Covers logits→softmax→decoding, greedy vs. sampling, temperature, top-k/top-p, beam search, repetition/stop controls, and the determinism/reproducibility product lens; weighted-dice analogy. 16/55.

- **2026-09-12** — **Category 3 continued: Chain-of-thought and extended thinking (17)** written and passed eval (14/14). Scoped as the CoT *behavior/technique* + the extended-thinking *feature*: why intermediate tokens help (more compute per token + autoregressive scratchpad + serialized decomposition), zero-shot → few-shot → trained reasoning spectrum, self-consistency (majority vote over sampled chains), and tradeoffs (latency/cost, not-always-better, faithfulness gap, hiding the raw chain). Reasoning models + test-time compute (18) linked not taught; few-shot (29), sampling (16), pretraining (06) linked. scratchpad/show-your-working analogy.

- **2026-09-12** — **Category 3 continued: Reasoning models and test-time compute (18)** written and passed eval (14/14). Scoped as the model *class* + the test-time-compute *scaling principle*: two axes of capability (train-time baked-in-once vs. test-time bought-per-query), reasoning models as same-architecture transformers post-trained (typically RL on verifiable math/code) to reason and use a budget, the forms of test-time compute (longer chains / parallel sampling+voting / search+verification), diminishing returns, when-not-to-reason, route-by-difficulty, and the cost shift to per-query inference. CoT/self-consistency/extended-thinking (17) linked not taught; RLHF contrast → (11); unit economics → (51). chess study-vs-clock analogy.

- **2026-09-12** — **Category 3 continued: Hallucination (19)** written and passed eval (14/14). Scoped as the *failure mode*, framed as intrinsic not a bug: four roots (objective is plausibility-not-truth, no internal knowledge boundary, training-data limits, alignment/RLHF rewarding confident answers→sycophancy cousin), the flavors (factual fabrication, fabricated citations, faithfulness failures with docs present, fluent-but-wrong reasoning), the manage-don't-cure toolkit (RAG grounding, citations, permit "I don't know," low temp for facts, task constraint, evals+human review), and product truths (can't be zeroed, confidence is uncalibrated, grounding changes the failure not removes it, why it's not "lying"). RAG (21), grounding/citations (27), RLHF (11), temperature (16), CoT (17), evals (45), pretraining (06) all linked, not taught. trivia-friend-who-can't-say-"I-don't-know" analogy.

## Changelog
- **2026-09-12** — **New Category 12 — Agent engineering added (4 topics: 56–59); Tool design (56) written and passed eval (14/14).** New `agent-engineering/` folder for the "how you wield the model" cluster, per user request (harness and loop kept as separate lessons, not merged). Syllabus bumped to 59 topics / 12 categories with a "what changed" note; README ToC gained a section 12 (56 linked, 57–59 marked *coming*). **56 scoped as the category opener** — the model is the *user* of your tools and reads only four authored surfaces (name / description / input schema / return value); naming+description as prompt-engineering (say *when* to use, disambiguate from neighbors); granularity as the key judgment call with the central principle **design tools around tasks, not API endpoints** (consolidate calls into task-shaped tools); schema design to prevent failures by construction (**never require an argument the model must guess** → it hallucinates one; enums, defaults, min required); return **signal not a data dump** (project down, cap volume, natural-language actionable errors as steering); and tool quality as a PM lever measured by evals (tool-selection accuracy + argument correctness). Tool calling (32), MCP (33), structured outputs (20), hallucination (19), context windows (05), prompt caching (31), cost (51), evaluation methods (45) all linked, not taught; forward-links to harness (57) and loop (58). workshop-apprentice-who-reads-only-labels analogy (distinct from 32's consultant-and-request-slip). 56/59.
- **2026-09-12** — **New "Dev surfaces" usage tier created — 7 lessons written in parallel (7 Opus writers), all passed `lesson-eval` (14/14; one on 2nd pass).** Resolves the "Dev-surface / API mechanics" coverage gap in USAGE-KEYWORDS (the terms were all ❌ gap). Written as their own tier in `dev-surfaces/` (one lesson per term, per user request — not merged into a single "Working with the API" lesson, and no relaxed eval bar). Each is built on a genuine PM-level spine so the thin topics clear the full concept gate honestly: **Workbench** (01) = workbench-to-production gap / design-not-testing surface; **Console** (02) = aggregate governance vs. per-request observability; **SDK** (03) = SDK-vs-raw-HTTP tradeoff + two-pin (model+SDK) reproducibility; **API endpoint** (04) = statelessness (resend full history every call → derives context windows + prompt caching); **API key** (05) = bearer secret, blast radius, store/scope/rotate; **Rate limits** (06) = two independent ceilings (TPM/RPM) + backoff-with-jitter; **Model ID** (07) = version pin, reproducibility vs. staying current. Wired into README (appendix section), USAGE-KEYWORDS (❌ gap → ✅ [DS] links + gaps section updated), and `dev-surfaces/README.md` index. Not part of the 55-topic count.
- **2026-09-12** — **BOOK COMPLETE (55/55): Categories 10 & 11 finished in one eight-agent parallel wave.** Eight concurrent Opus writer agents, one per lesson, each scope-partitioned against its siblings and the rest of the book and each self-passing `lesson-eval` (14/14). **Category 10 — production & ops:** **48 Production LLM architecture** (category opener — the model is one component; end-to-end request path gateway→orchestration→context→prompt→model→guardrails→stream, model routing/gateways + fallback, app-tier exact/semantic caching distinct from provider-side prompt/KV caching, reliability patterns, statelessness; airport analogy). **49 Inference optimization** (the model *server's* speed/cost, distinct from 48 — TTFT/prefill vs. TPOT/decode, throughput↔latency dial, continuous/in-flight batching, KV cache as the batch-size ceiling + PagedAttention, speculative decoding; rush-hour-highway (one trip's time vs. cars-per-hour) analogy; KV mechanism/quantization/distillation linked not taught). **50 LLM observability** (quality/output observability as a second axis beyond HTTP-200 health — tracing chains as spans, system vs. output signals, user feedback + online eval on sampled traffic, drift; the offline-eval↔online-observability loop; car dashboard + dashcam analogy). **51 Cost and unit economics** (token pricing + input/output asymmetry, cost-per-successful-task as the right denominator, drivers→levers mapping, the linear-marginal-cost scaling trap; metered-electricity-bill analogy). **Category 11 — product & strategy:** **52 AI product metrics** (category opener — the third measurement layer after model evals + system observability; task-success/acceptance/containment/trust/guardrail families, leading vs. lagging, Goodhart, offline↔online divergence; car-spec-sheet-vs-commute analogy). **53 Build vs. buy** (two stacked decisions — model layer buy→host→fine-tune→train + application layer buy vs. build — across seven axes; buy-first-descend-for-a-reason; rent/buy/build-a-home analogy). **54 Data moats and feedback loops** (the usage→data→better-product flywheel; most data is *not* a moat — needs proprietary + hard-to-replicate + product-improving; cold start; bias-amplification/model-collapse failure modes; worn-footpath analogy). **55 The iron triangle** (capstone — cost/latency/quality trade because quality is bought with compute; the book's levers mapped onto the triangle's edges; "which corner is this use case?"; the Pareto reframe that a better technique moves the whole frontier; three-fader mixing-board analogy). Orchestrator consistency pass: all new sibling and cross-category links resolve; **book-wide broken-link scan now 0** after fixing three pre-existing stale links unrelated to this wave (lesson 02 embeddings `25-reranking`→`26-reranking` after the earlier 25↔26 swap; lesson 35 agent-memory two same-dir `21-rag`/`23-vector-databases` links corrected to `../retrieval-knowledge/...`). README ToC already carried all eight entries; verified the airport/highway/dashboard/electricity-meter/spec-sheet/home/footpath/mixing-board analogies are distinct from each other and the existing set. **All 11 categories, all 55 lessons, written and passed.**
- **2026-09-12** — **Category 4 (retrieval & knowledge) completed in a parallel wave: Hybrid search (25), Reranking (26), Grounding and citations (27)** — all written and each self-passed `lesson-eval` (14/14). Three concurrent Opus writer agents, one per lesson, scope pre-partitioned along the two failure modes lesson 24 named so the three cross-link instead of overlapping: **25 owns the *relevant-but-not-similar* fix** — dense/semantic + sparse/lexical (BM25) run in parallel then fused; why the two fail on opposite inputs (meaning/paraphrase vs. exact identifiers like error codes, SKUs, part numbers that embeddings structurally blur); fusion via Reciprocal Rank Fusion (rank-based, dodges incompatible score scales) vs. weighted/alpha blending; tradeoffs (second index, tuning); BM25 kept at intuition depth; two-bookstore-clerks analogy. **26 owns the *similar-but-not-relevant* fix** — the second-stage reranker, the retrieve-wide-then-rerank-narrow (recall→precision) pattern, the **bi-encoder vs. cross-encoder** mechanism deferred here from lesson 24 (joint attention → O(candidates) cost → why only on the shortlist), where rerankers come from (hosted APIs / open cross-encoders), metric wins (Precision@k, MRR, NDCG), and the hard ceiling that a reranker can only reorder what retrieval surfaced; matchmaker profile-cards-vs-real-dates analogy. **27 closes the category** — grounding (answer *derived from* context, not merely context-present; parametric-memory fallback; the three levers: answer-only, permission to abstain, constrain outside knowledge), per-claim citations and why checkability changes the product, the **faithfulness-vs-correctness** distinction, the three failure modes (parametric leak, citation hallucination, grounded-but-wrong), light groundedness/attribution measurement, and the retrieve→hybrid→rerank→ground→cite pipeline; newsroom standards-desk analogy. Orchestrator did the consistency pass: sibling links 24↔25↔26↔27 resolve; hallucination (19), structured outputs (20), RAG (21), vector DBs (23), retrieval (24), embeddings (02), attention (03), evals (45) all linked, not taught; three analogies distinct from each other and the existing set. README ToC already carried these entries; only status/count updated here. Category 4 now complete (21–27); on disk now 1–47 = **47/55**.
- **2026-09-12** — **Category 9 (evaluation) completed in a parallel wave: Evaluation methods (45), Benchmarks and their limits (46), Regression testing (47)** — all written and each self-passed `lesson-eval` (14/14). Three concurrent Opus writer agents, one per lesson, scope pre-partitioned so the three don't overlap and cross-link instead: **45 owns the measurement toolkit** (why LLM eval is hard — open-ended/non-deterministic/multi-dimensional; the three families on a cheap-shallow→expensive-faithful spectrum: reference metrics incl. why BLEU/ROUGE fail on paraphrase, human eval as gold standard, **LLM-as-judge as the workhorse + its position/verbosity/self-preference biases and the calibrate-against-humans rule**; pointwise vs. pairwise; offline vs. online; the golden set as the foundation and ratchet; multiple-choice-key-vs-essay-rubric-panel analogy; category-9 opener). **46 owns public benchmarks + why they mislead** (what a benchmark/leaderboard is and is for, MMLU/GSM8K/HumanEval/arena named as examples, the four limits — contamination/data-leakage, saturation, construct validity, Goodhart's law — plus static/aging, landing on "coarse screen for shortlisting, never a substitute for your own eval"; SAT/teaching-to-the-test analogy — reserved to this lesson). **47 owns detecting breakage across your own changes** (silent/delayed regressions with no compiler error; harder than normal regression testing because outputs are non-deterministic + open-ended so the assertion becomes "clears a quality bar" not `==`; prompts-as-code + eval-suite-as-CI gating ship/block against a golden set; grow-the-set-per-bug, score-vs-baseline delta, run-N-times-for-a-pass-rate + drift budget, and pin-the-model-version + re-run-before-migrating; fix-one-pipe-then-turn-on-every-tap analogy). Orchestrator did the consistency pass: sibling links 45↔46↔47 all resolve; **fixed one broken forward-link** (47 pointed RAG at a non-existent `../04-rag/` → corrected to `../retrieval-knowledge/rag.md`); LLM-as-judge, golden sets, and benchmarks each owned by exactly one lesson and referenced-not-retaught by the others; cross-category links to unwritten lessons (28 now written; 50/51/52/53) are forward-links per the book's convention; three analogies distinct from each other and from the existing set. README ToC already carried these entries; only status/count updated here. On disk now includes 45–47 = **44/55**.
- **2026-09-12** — **Category 8 (safety & trust) completed in a parallel wave: Guardrails (41), AI security (42), Privacy and PII (43), Responsible AI (44)** — all written and each self-passed `lesson-eval` (14/14). Four concurrent Opus writer agents, one per lesson, scope pre-partitioned so the four lenses don't overlap and cross-link instead: **41 owns the runtime control layer** (input/output guard architecture, deterministic-rule vs. model-based/moderation/LLM-as-judge guards, the enforcement jobs, false-positive↔false-negative + latency + defense-in-depth tradeoffs; bouncer + coat-check analogy; category-8 opener). **42 owns the adversarial lens** (the no-trust-boundary root cause, direct vs. indirect prompt injection, jailbreaking-vs-injection, the lethal trifecta, exfiltration/poisoning/extraction at overview depth, defense-in-depth posture; eager-temp-receptionist-who-can't-tell-boss-from-forged-note analogy). **43 owns accidental personal-data handling across the LLM lifecycle** (what PII is incl. quasi-identifiers, the four leak paths — memorization / logging+training / retrieval stores / cross-tenant, the mitigation ladder minimize→redact→retention/ZDR→do-not-train→isolation→self-host, and the GDPR frame incl. why right-to-deletion is hard once data is in the weights; friend-with-perfect-memory-who-gossips analogy). **44 is the capstone — the values/governance lens** (bias entry + feedback loop, the fairness-impossibility tradeoff, transparency/model cards, accountability + human oversight, misuse/dual-use, EU AI Act / NIST AI RMF at name depth; hospital ethics-board analogy). Orchestrator did the consistency pass: sibling links 41↔42↔43↔44 resolve; **fixed one broken forward-link** (42 pointed fine-tuning at a non-existent `07-training-adaptation/38-fine-tuning.md` → corrected to `../model-behavior-training/fine-tuning.md`); all other links to written lessons resolve, cross-category links to unwritten lessons (20 structured outputs is now written; 27 grounding, 45 evaluation still forward-links per the book's convention); four analogies distinct from each other and from the existing set. README ToC already carried these entries; only status/count updated here. On disk now includes 41–44 = **41/55**.
- **2026-09-12** — **Category 6 (agents) completed in a parallel wave: Planning and orchestration (36) and Multi-agent architectures (37)** — both written and each passed `lesson-eval` (14/14). Two concurrent Opus writer agents, scope pre-partitioned so **36 owns orchestrating *steps within one agent's plan* and 37 owns orchestrating *multiple agents*** (the two cross-link each other on that exact boundary rather than overlapping). **36** teaches the reactive-loop failure on long-horizon tasks (drift, redone work, compounding error), the central **ReAct vs. Plan-and-Execute** tradeoff (plan-first buys structure/global-view/cheap-execution/auditability but goes stale; ReAct adapts per step but drifts and re-derives the plan each iteration), **replanning** ("a plan is a hypothesis, not a contract"), and the orchestration-ownership spectrum (deterministic controller vs. model-as-orchestrator) with the plan as a **DAG** exposing sequential vs. parallel branches + state passing; five-city-trip itinerary-vs-winging-each-leg analogy. **37** teaches why a single agent hits ceilings (context bloat, tool confusion, jack-of-all-trades prompt, no parallelism), the four patterns each with a Mermaid diagram (orchestrator–worker, sequential pipeline, parallel fan-out+aggregate, generator↔critic), handoff mechanisms with the load-bearing failure that **context degrades at every handoff**, and the honest tradeoffs landing on **most tasks don't need multi-agent**; kitchen-brigade analogy. Loop/compounding-error (34), memory (35), tool calling (32), prompt caching (31), context windows (05), prompt engineering (28), AI-security (42), cost (51) all linked, not taught. README ToC already carried both entries; only status/count updated here. Category 6 now complete (32–37).
- **2026-09-12** — **Category 7 (multimodal) completed in a parallel wave: Vision-language models (38), Audio and speech (39), Multimodal product surfaces (40)** — all written and each self-passed `lesson-eval` (14/14). Three concurrent Opus writer agents, one per lesson, with scope pre-partitioned so the mechanisms live in 38/39 and the product/UX lens lives in 40 (40 and 39 link out to the mechanisms rather than re-teaching them). **38** owns how images enter an LLM (patchify → vision encoder/ViT → projection into token space), VLM architecture, the core tasks (captioning, VQA, OCR/document understanding, grounding), the resolution↔token-cost tradeoff, and four failure modes (spatial/counting, small-text/dense docs, hallucinated visuals, prompt-injection via text-in-images); currency-exchange-booth analogy. **39** owns ASR + TTS, audio→model at overview depth (waveform→spectrogram/audio tokens), end-to-end speech-to-speech and why it wins (latency, prosody, barge-in), and the voice tradeoffs (latency budget, streaming vs. batch, WER's uneven failures, TTS naturalness + voice-cloning consent/misuse); sheet-music-vs-by-ear (jazz) analogy. **40** owns the product surfaces (snap-a-photo, document/screenshot understanding, voice, image/video generation, any-to-any), the effort-vs-ambiguity UX dynamic, a per-surface "does it earn its place" decision framework, and the five PM-owned tradeoffs (cost/latency, evaluation, accessibility, privacy, silent-misread trust); restaurant-ordering-channels analogy. Orchestrator did the consistency pass: sibling links 38↔39↔40 resolve, all links to already-written lessons resolve, cross-category links to unwritten lessons are forward-links per the book's convention, three analogies distinct from each other and from the existing set. README ToC already carried these entries; only status/count updated here. On disk now includes 38, 39, 40 = **33/55**.
- **2026-09-12** — **Category 5 completed: Prompt engineering (28) written and passed eval (14/14).** ("Parallelize category 5" collapsed to a single lesson — 29/30/31 were already done, so 28 was the only remaining topic; no fan-out needed.) Scoped as the category opener / umbrella discipline: prompting *steers a frozen model, it does not add knowledge or capability* (the load-bearing thesis); the input as the only run-time lever; the techniques framed as **principles, each answering a specific under-specification** (be specific, add structure/delimiters, assign a role, constrain output, give an "I don't know" out, show examples, let it think, decompose); system-vs-user prompt roles + placement ("lost in the middle"); the empirical/iterative craft + model-version fragility + over-engineering; and the ceiling (can't add knowledge→RAG, can't cheaply encode stable high-volume→fine-tuning, can't exceed capability, is an attack surface→prompt injection). Few-shot (29), CoT (17), structured outputs (20), hallucination (19), RAG (21), fine-tuning (07), prompt injection/AI-security (42), context windows (05), agent memory (35), regression testing (47) all linked, not taught. brilliant-but-literal-freelancer-brief analogy (distinct from 29's temp-with-sample-forms). Category 5 now complete.
- **2026-09-12** — **Category 4: Retrieval (24) written and passed eval (14/14), + order swap 25↔26.** Retrieval scoped as query-time logic — embed-query/score/top-k/threshold, cosine-vs-dot-vs-Euclidean (direction not magnitude), top-k as the recall dial, query rewriting/expansion, Recall@k/Precision@k/MRR — anchored on the *similarity ≠ relevance* thesis (similar-but-not-relevant → reranking; relevant-but-not-similar → hybrid). Reranking/hybrid/vector-DB/chunking/embeddings all linked, not taught (removed a leaked "cross-encoder" ref → deferred to reranking). recruiter/résumé analogy. **Swap:** per user request, Hybrid search is now **25** and Reranking is now **26** (flow: get candidates → reorder them); applied to README, syllabus (+ "what changed" note), tracker rows, and all cross-links in lessons 21/23/24. Files 25/26 unwritten, so this was a plan renumber only.
- **2026-09-12** — **Category 6 continued: Agent memory (35) written and passed eval (14/14).** Scoped as state *around* the stateless model — memory is a system you build, not a model feature; short-term (=context window, ephemeral) vs. long-term (external store, persists); long-term memory ≈ RAG over the agent's own experience; the write/read cycle as designed policies; window management via summarization (lossy) or retrieval; episodic/semantic/procedural taxonomy kept light; tradeoffs (what-to-remember is curation, stale memory persists+poisons→needs invalidation, retrieval quality caps it, per-step token cost, persistence=PII decision). Agentic (34), context windows (05), RAG (21), vector DBs (23), prompt caching (31), PII (43) linked, not taught. Memento-amnesiac + desk + filing-cabinet analogy. Category 6 needs only 36, 37.
- **2026-09-12** — **Category 5 (reverse) continued: Few-shot and in-context learning (29) written and passed eval (14/14).** Scoped as ICL + the shot vocabulary — zero/one/few-shot spectrum; in-context learning as inference-time, weights-frozen, ephemeral pattern-completion (mechanism flagged as still-debated, not stated as fact); few-shot teaches *task/format, not facts*; few-shot-vs-fine-tuning decision on volume × stability; examples-are-the-spec sensitivity (quality, selection, order/majority-label bias, diminishing returns). Prompt engineering (28), fine-tuning (07), RAG (21), prompt caching (31), context windows (05), attention (03), pretraining (06) all linked, not taught. temp-worker-with-sample-forms analogy. Category 5 now needs only 28 (Prompt engineering).
- **2026-09-12** — **Category 6 continued: Agentic systems (34) written and passed eval (14/14).** Scoped as the loop itself — an LLM in a loop with tools + a goal, reason→act→observe→repeat; the defining property is *autonomy over control flow* (model decides steps + when to stop); workflow↔agent as a spectrum with the "least autonomy that works" rule; compounding error as the signature failure (0.95^10≈60%), plus unpredictability, multiplied cost/latency, runaway loops and execution-layer guardrails. Tool calling (32), CoT (17), memory (35), planning (36), multi-agent (37), prompt caching (31), AI-security (42) all linked, not taught. intern-with-a-goal vs. checklist analogy. On disk now 1–17, 21, 22, 30, 31, 32, 33, 34 = **23/55**.
- **2026-09-12** — **Category 5 (reverse) continued: KV caching (30) written and passed eval (14/14).** Scoped as the within-a-generation mechanism — the autoregressive recompute waste; cache K and V (not Q, which is consumed once); prefill (parallel, compute-bound, sets TTFT) vs. decode (sequential, memory-bandwidth-bound, sets tok/s); the KV cache as the *memory* bottleneck growing linearly with context length × batch → context-vs-throughput tradeoff; GQA/MQA/PagedAttention/sliding-window/KV-quant as cache-shrinking fixes (depth deferred to inference-optimization 49); lossless vs. lossy distinction. Attention Q/K/V (03), autoregressive loop (16), prompt caching (31), quantization (14) all linked, not taught. corkboard-of-index-cards analogy. On disk now 1–16, 21, 22, 30, 31, 32, 33 = **21/55**.
- **2026-09-12** — **Category 6 continued: MCP (33) written and passed eval (14/14).** Scoped as the standard *around* tool calling, not a model capability — the N×M→N+M integration problem; "USB-C for AI tools"; host/client + server + protocol roles; dynamic discovery as the new trick vs. hardcoded schemas; tools/resources/prompts at overview depth only; tradeoffs (security as active-code-with-access + injected resources, standardizes access not judgment, young standard, extra moving part). Tool calling (32), prompt injection/AI-security (42), context windows (05) linked, not taught. On disk now 1–16, 21, 31, 32, 33 = **20/55**.
- **2026-09-12** — **Category 5 started in reverse order: Prompt caching (31) written and passed eval (14/14).** Scoped as the productized, persisted form of the KV cache — same mechanism, wider scope (across requests vs. within one generation); the prefix rule and stable-to-variable ordering; three-price economics (write premium ~1.25–2×, read ~90% off) as a break-even bet on reuse; TTL/minimum-length/best-effort operational rules; latency (prefill-skip) win; agent-loop and long-chat as ideal workloads; cache-hit-rate as the metric. KV caching (30), few-shot (29), RAG (21), retrieval (24), tool calling (32), context windows (05), agents (34) all linked, not taught. prep-kitchen base-steps analogy. On disk now 1–16, 21, 31, 32 = **19/55**.
- **2026-09-12** — **Category 6 started: Tool calling / function calling (32) written and passed eval (14/14).** Scoped as the category opener — the model *requests*, your code *executes*; the tool-calling loop; learned-in-post-training tool selection driven by descriptions; failure modes (wrong tool / hallucinated args / silent non-call / multiplied latency+cost / security of untrusted tool calls); consultant-and-request-slip analogy. Agents/MCP/multi-agent/structured-outputs/hallucination/prompt-caching/AI-security all linked out, not taught inline. **Note:** written concurrently with other sessions; on-disk passed lessons are now 1–15, 16, 21, 32 = **18/55** (the per-lesson "16/55" stamps below are racy and predate this).
- **2026-09-11** — Scaffold created (README ToC, this tracker, 11 folders, ML vocabulary primer). Lesson 1 (Transformers) written and passed `lesson-eval`.
- **2026-09-11** — Category 1 written in parallel (5 Opus writer agents), independent reviewer pass (consistency + links verified).
- **2026-09-11** — Standard revised: ASCII → **Mermaid** diagrams; **every** interview Q&A carries a `[!TIP]` rationale; vocab primer reworked (Mermaid + grouped tables with Example column); added `KEYWORDS.md`. All 6 category-1 lessons retrofitted.
- **2026-09-11** — **Dependency-clean reorder of the whole syllabus** (see `syllabus.md`). Foundations renumbered tokens → embeddings → attention → transformers → context windows → pretraining; categories 2–6 resequenced; files renamed and all cross-links rewritten.
- **2026-09-11** — New standard: one memorable **analogy** per lesson (Picture → Mapping → **Why it holds** → Say it like this → Where it breaks). Added the analogy section to all 6 category-1 lessons and **reworked Transformers** into a capstone (assumes tokens/embeddings/attention; added the "why depth builds abstraction" section + editorial-team analogy).
- **2026-09-12** — **Category 4 started: RAG (21) written and passed eval.** User chose to jump to category 4 (retrieval) ahead of category 3 (reasoning, 16–20), which is deferred. RAG scoped as the category opener — retrieve-then-generate architecture, RAG-vs-fine-tuning decision, the "retrieval quality caps answer quality" iron law, and what RAG doesn't fix — with chunking/vector-DBs/retrieval/reranking/hybrid-search/grounding all linked out, not taught inline. 16/55.
- **2026-09-12** — **Category 2 complete (9 lessons, 7–15).** Written by two parallel Opus waves (fine-tuning, PEFT, LoRA, instruction tuning, RLHF; then DPO, distillation, quantization, MoE), each self-passing all 14 rubric items. Reviewer pass done in-session (the Opus reviewer agent hit the session limit): links verified, concept ownership confirmed (low-rank→LoRA, reward model→RLHF, DPO contrasts not re-teaches), nine analogies distinct. 15/55.
